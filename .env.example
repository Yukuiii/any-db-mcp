# 数据库类型：mysql / postgresql / sqlite
DB_TYPE=mysql

# 连接配置（MySQL / PostgreSQL 使用）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=

# SQLite 文件路径（仅 SQLite 使用）
DB_FILEPATH=

# 只读模式（true 时禁止执行写操作）
READONLY_MODE=false
